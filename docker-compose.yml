services:
  sandbox-api:
    build:
      context: ./sandbox-api
      dockerfile: Dockerfile
    ports:
      - "6532:6532"
    env_file:
      - .env
    volumes:
      - sandbox_data:/app/data
    restart: unless-stopped

volumes:
  sandbox_data:
